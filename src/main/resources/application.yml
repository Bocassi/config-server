
server:
  port: 8888

spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/Bocassi/config-repo.git
          clone-on-start: true
          default-label: develop

  profiles:
    active:

  application:
    name: config-server